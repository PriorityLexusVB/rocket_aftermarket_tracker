# Copy this file to .env.local and fill in your Supabase project values.
# For Playwright E2E, prefer a separate .env.e2e.local with NON-production Supabase.
# NEVER commit your real keys. .env.local and .env.e2e.local should stay untracked.

# Tip: Supabase “project ref” is the subdomain in https://<ref>.supabase.co.
# Keep E2E and production clearly separated:
# - .env.e2e.local (Playwright) -> E2E project (non-production)
# - Vercel Production env vars -> production project
# `pnpm release:check` refuses to run if VITE_SUPABASE_URL contains the production ref ogjtmtndgiqqdtwatsue.

# Supabase client (browser)
VITE_SUPABASE_URL=https://YOUR-PROJECT.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-KEY

# Feature Flags (optional)
VITE_DEAL_FORM_V2=true
VITE_SIMPLE_CALENDAR=
VITE_ACTIVE_SNAPSHOT=

# Playwright E2E (used by global.setup.ts / playwright.config.ts)
PLAYWRIGHT_BASE_URL=http://127.0.0.1:5174
E2E_EMAIL=tester@example.com
E2E_PASSWORD=your-password

# Optional: Postgres connection string for local DB scripts (report/cleanup/seed)
# Tip (WSL/network-restricted): prefer the Supabase "Session pooler" connection string and include sslmode.
E2E_DATABASE_URL=postgresql://user:password@YOUR-POOLER.pooler.supabase.com:5432/postgres?sslmode=require

# Optional: Supabase admin for local bootstrap/seed scripts (do NOT commit real values)
SUPABASE_URL=https://YOUR-PROJECT.supabase.co
SUPABASE_SERVICE_ROLE_KEY=YOUR-SERVICE-ROLE-KEY
TEST_USER_EMAIL=tester@example.com
TEST_USER_PASSWORD=your-password
TEST_USER_NAME=Test User
