name: Supabase Migrate Production

on:
  push:
    branches: ['main']
    paths:
      - 'supabase/migrations/**'
      - 'supabase/config.toml'
  workflow_dispatch:

concurrency:
  group: supabase-migrate-prod
  cancel-in-progress: false

jobs:
  migrate:
    runs-on: ubuntu-latest
    environment: supabase-production

    env:
      SUPABASE_ACCESS_TOKEN: ${{ secrets.SUPABASE_ACCESS_TOKEN }}
      SUPABASE_DB_PASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
      SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}

    steps:
      - name: Check required secrets
        id: secrets_check
        run: |
          set -euo pipefail
          missing=()
          if [ -z "$SUPABASE_ACCESS_TOKEN" ]; then missing+=("SUPABASE_ACCESS_TOKEN"); fi
          if [ -z "$SUPABASE_DB_PASSWORD" ]; then missing+=("SUPABASE_DB_PASSWORD"); fi
          if [ -z "$SUPABASE_PROJECT_REF" ]; then missing+=("SUPABASE_PROJECT_REF"); fi
          if [ ${#missing[@]} -gt 0 ]; then
            echo "missing_secrets=true" >> "$GITHUB_OUTPUT"
            echo "Supabase secrets not available (${missing[*]}); skipping production migrate."
            echo "::warning::Supabase production migrate skipped because required secrets are missing: ${missing[*]}"
            echo "::warning::Fix: Configure these in GitHub -> Settings -> Environments -> supabase-production -> Environment secrets."
          else
            echo "missing_secrets=false" >> "$GITHUB_OUTPUT"
            echo "Supabase secrets present."
          fi

      - name: Skip when secrets unavailable
        if: steps.secrets_check.outputs.missing_secrets == 'true'
        run: |
          echo "Skipping Supabase production migrate because Supabase secrets are not configured/available for this workflow context."

      - uses: actions/checkout@v4
        if: steps.secrets_check.outputs.missing_secrets == 'false'

      - uses: supabase/setup-cli@v1
        if: steps.secrets_check.outputs.missing_secrets == 'false'
        with:
          version: 2.65.5

      - name: Show Supabase CLI version
        if: steps.secrets_check.outputs.missing_secrets == 'false'
        run: |
          echo "=== Supabase CLI Version ==="
          supabase --version

      - name: Disable config.toml temporarily
        if: steps.secrets_check.outputs.missing_secrets == 'false'
        run: |
          echo "=== Disabling config.toml to avoid parse errors with pinned CLI version ==="
          if [ -f "supabase/config.toml" ]; then
            mv supabase/config.toml supabase/config.toml.disabled
            echo "✓ Renamed config.toml to config.toml.disabled"
          else
            echo "✓ config.toml not found, skipping"
          fi

      - name: Link Supabase project
        if: steps.secrets_check.outputs.missing_secrets == 'false'
        run: |
          set -euo pipefail
          echo "=== Linking to Supabase Project ==="
          echo "Project ref: $SUPABASE_PROJECT_REF"
          supabase --debug link --project-ref "$SUPABASE_PROJECT_REF" --password "$SUPABASE_DB_PASSWORD"
          echo "✓ Successfully linked to project"

      - name: List pending migrations
        if: steps.secrets_check.outputs.missing_secrets == 'false'
        run: |
          set -euo pipefail
          echo "=== Migration Status ==="
          echo "Local migrations detected: $(ls -1 supabase/migrations/*.sql 2>/dev/null | wc -l)"
          supabase migration list || echo "Warning: Could not list migrations"

      - name: Apply migrations to production
        if: steps.secrets_check.outputs.missing_secrets == 'false'
        run: |
          set -euo pipefail
          echo "=== Applying Migrations to Production ==="
          echo "Command: supabase --debug db push --yes"
          if supabase --debug db push --yes; then
            echo "✓ Migrations applied successfully"
          else
            echo "✗ Migration failed - check logs above for details"
            exit 1
          fi

      - name: Restore config.toml
        if: always() && steps.secrets_check.outputs.missing_secrets == 'false'
        run: |
          echo "=== Restoring config.toml ==="
          if [ -f "supabase/config.toml.disabled" ]; then
            mv supabase/config.toml.disabled supabase/config.toml
            echo "✓ Restored config.toml from config.toml.disabled"
          else
            echo "✓ config.toml.disabled not found, skipping"
          fi
