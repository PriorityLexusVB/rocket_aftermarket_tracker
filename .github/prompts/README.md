# Prompts

See **INDEX.md** for the recommended prompt list + when to use each.

## Notes
- These are VS Code Copilot **prompt files** (`*.prompt.md`) stored in `.github/prompts/`.
- Frontmatter uses supported fields (`name`, `agent`, `description`, `argument-hint`, `model`, `tools`).
